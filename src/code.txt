/* Example Program */

/* Variable Declarations */
static int counter = 0;
float ratio[10] = {0.1, 0.2, 0.3};
char letter = 'A';
string name = "Language";
float b = ratio[5];

/* Function to calculate the sum of two integers */
static func sum(int a, int b) -> int {
    return a + b;

}

func print(string s) -> void {
    "Do something";
}


/* Class Declaration*/
class Animal {
    string species;

    func Animal(string inputSpecies) -> void {
        species = inputSpecies;
    }

    static func describe() -> void {
        print("This animal is a " + species);
    }

    action showSpecies(Animal animal) -> void : "Spec {animal.toString}" {
        species;
    }
}

/*Class Inheritance*/
class Dog extends Animal {
    int a = 0;
}

/*Main Block*/

    /* Using the sum function*/
    int result = sum(5, 10);
    print("Sum: ");

    print("Hello, World!");
    string dog = "put dog here";
    string cat = "put cat here";
    string animals[2] = {dog, cat};

    /*Working with an array*/
    loop for animal in animals {
        print(animal);
    }

    /*Conditional statement*/
    if (counter > 0) {
        print("Counter is positive.");
    }

    /*Class and object usage*/
    string myDog = "put dog here";
    Animal.describe();
    Dog.describe();