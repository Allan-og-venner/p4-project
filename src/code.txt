/* Example Program */

/* Variable Declarations */
static int counter = 0;
float ratio[10] = {0.1, 0.2, 0.3};
char letter = 'A';
string name = "Language";

/* Function to calculate the sum of two integers */
static func sum(int a, int b) -> int {
    return a + b;
}


/* Class Declaration*/
class Animal {
    string species;

    func Animal(string species) -> void {
        this.species = species;
    }

    func describe() -> void {
        print("This animal is a " + this.species);
    }

    action showSpecies(Animal animal) -> void : "Spec {animal.toString}" {
    	this.species;
    }

}

/*Class Inheritance*/
class Dog extends Animal {
int a = 0;
}

/*Main Block*/

    /* Using the sum function*/
    int result = sum(5, 10);
    print("Sum: " + result);

    /* Display action*/
    display("Hello, World!");

    /*Working with an array*/
    loop for animal in animals {
        print(animal);
    }

    /*Conditional statement*/
    if (counter > 0) {
        print("Counter is positive.");
    }

    /*Class and object usage*/
    Dog myDog = new Dog();
    myDog.describe();
