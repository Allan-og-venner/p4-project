
action play(Card card) -> void : "Play" {
	print("playing");
}

action discard(Card card) -> void : "Discard" {
	print("discarding");
}

action shoot(string player) -> void : "Shoot" {
	print("Shooting");
}

Card card1 = new Card();
card1.ID = "red 5";

allowAction(discard(card1));
allowAction(shoot("simon"));
displayAllowedActions();
